export interface UserAvatarStoryProps {
  size: number; // Kích thước của hình ảnh, ví dụ: '50px', '100px'
  src: string;  // Đường dẫn tới ảnh đại diện
  haveSeenBefore: boolean; // Đã thấy trước đó hay chưa
}
